---
title: Publication record
order: 4
---

# Publication record

The owner authorized GitHub upload and GitHub Pages publication on 21 September 2026. The owner clarified that the main publication is the full Jev vs. Laya comparison. This release includes Banking77, five additional public workflow tasks, the aggregate results of the exploratory curated workflow pilot, and the six-source moderation study. The standalone moderation report remains available at `moderation.html`. The new matched Jev moderation evaluation is documented separately as a secondary analysis.

The original research workspace remains unchanged by Git operations. This separate publication checkout uses the feature branch `research/full-report`, also the source branch for GitHub Pages. There is no main/master push, PR merge, workflow dispatch, or change to the user's home Git file. The new repository has no pre-existing project directives; inherited branch discipline applies. The original no-Git exception continues for the research workspace.

Prediction records retain their historical paths and recorded hashes for auditability. Those paths identify the original machine; they are not required to exist for saved-prediction scoring. Public Banking77 source text is included with its license. Other dataset text, private workflow text, model weights, authentication credentials and raw telemetry requests are excluded. Dataset and upstream model terms remain their own.

`SHA256SUMS.json` is the checksum inventory of the earlier moderation review bundle, retained as historical evidence. `evidence/workflow-review-SHA256SUMS.json` records the earlier workflow bundle. `PUBLIC_SHA256SUMS.json` is the publication file inventory; it excludes itself and Git metadata. The public presentation adds charts and reproduction links without changing recorded predictions or scores.
